def main():
    fenrir = input()

    # Since fenrir's howl will always be valid
    # We only need to return a howl with one character longer
    # 'O' is always valid
    print(fenrir + 'O')


if __name__ == '__main__':
    main()
